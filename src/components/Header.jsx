const Header = ({ children }) => {
  return (
    <div className="App">
      <header className="App-header">
        {children}
      </header>
    </div>
  );
};

export default Header;

export const Heading = () => {
    return <h1>RIZZYAD MVS</h1>
}
